using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    /*[SerializeField]
    Transform bossLevelStarter;
    [SerializeField]
    LayerMask playerLayer;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        Debug.DrawRay(bossLevelStarter.position, Vector3.back * 14f, Color.red, Mathf.Infinity);
        if (Physics.Raycast(bossLevelStarter.position, Vector3.back, 8f, playerLayer))
        {
            PlayerController.instance.isFightMode = true;
        }
    }
    */


}
